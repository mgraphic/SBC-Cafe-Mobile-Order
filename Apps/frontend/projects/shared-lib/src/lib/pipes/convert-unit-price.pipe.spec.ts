import { ConvertUnitPricePipe } from './convert-unit-price.pipe';

describe('ConvertUnitPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertUnitPricePipe();
    expect(pipe).toBeTruthy();
  });
});
